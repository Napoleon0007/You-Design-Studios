__pycache__/
*.pyc
.DS_Store
data/*.db
data/files/
data/outbox/
data/gelato_catalog.json
data/gelato_*.json
# …but the VERIFIED apparel catalog IS needed at runtime (catalog.py reads it),
# so it must ship to Railway. Without it the store loads with zero products.
!data/gelato_apparel.json
.env
venv/
.venv/
.wrangler/
# Raw source mockup videos (large; processed web copies live in static/media/).
# Anchored to repo root so it does NOT match the processed clips in static/media/.
/_users_*.MP4
/*.MP4
# Source/original Meshy renders + garment source photos (~178MB of 12–60MB GLBs).
# Only the Draco-compressed copies in static/models/ are used at runtime; shipping
# these originals balloons `railway up` to ~200MB and times the upload out.
/Garment photos for Meshy/
# Stray mockup template that isn't referenced anywhere on the site.
/brown-professional-premium-unisex-mockup-hood-design-template-*.webp
# Reference screenshots + raw design SOURCE + parked designs — not needed at
# runtime (the live library is data/designs/); keep `railway up` lean.
/2nd draft of printing website/
/Clothing art/
data/designs_parked/
static/v2/_cardgen.html
/tools/
/lacoste-taste/
# OTC supplier reference docs (~25MB of PDFs/catalogues) — not website runtime.
/OCT printers info and cataloque/
# Final Cut Pro marketing project — not part of the website.
/TRUEF_Marketing.fcpxml

.dev.vars*
!.dev.vars.example
!.env.example
