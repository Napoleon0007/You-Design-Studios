# Next-chat handoff — TRUeF Studios (2026-06-21)

> Left here so the next chat (and the other two windows editing this repo) can pick up + comment.
> **Committed as a snapshot** (Luke asked to commit while the other two windows are still working) —
> this commit bundles my spin fix **plus** the other windows' WIP (studio backdrop + `_frozen`).
> **NOT deployed** — the live site is still the prior clean build until someone runs `railway up`.
> The other windows can keep editing; their next saves are simply new uncommitted changes on top.

## ✅ LIVE on Railway (deployed + verified this session)
`https://you-design-studios-production.up.railway.app` — service `You-Design-Studios`.
- **TRUeF rebrand**: one-word **TRUeF** logo + framed-T mark + favicon; clean theme.
- **v2 is the public homepage** (`app.py` `/` serves `static/v2/index.html`); old dark page at **`/classic`**.
- **Landing rolodex** ("The Collection") of 2D shirt cards (`static/v2/cards/`, generated by `gen_cards.py`);
  the 3D MP4 clips kept as their own "In motion" cards. Same rolodex picker in the studio.
- **Instant-shirt poster** (`static/v2/hero-poster.png`) so the hero is never blank; cross-fades to 3D.
- **Smooth garment carousel**: engine now **caches + preloads** models (no re-download per swap),
  garments **materialise in** (blur+scale+fade), **6s** cadence, **every garment printed** with a cool set.
- **Women's filter** (All / Men / Women) in the studio (`gender` plumbed through `catalog.py`).
- Library curated to **17** clean Gelato-safe designs (penguin + moth removed); category folders
  `data/designs/{womens,kids,more-types}/` exist for later.
- Deploy = `railway up --detach --service You-Design-Studios` + `git push` (PAT). ⚠️ rapid back-to-back
  deploys sometimes don't land on the first poll — re-poll / one more `railway up`.

## ⏳ UNCOMMITTED in the working tree — do NOT lose / do NOT clobber

**(A) My spin-fix — `static/js/garment3d.js` (READY, needs final verify + deploy):**
Bug Luke reported: dragging the landing garment glitched / got stuck / moved erratically.
Two causes, both fixed:
1. Auto-spin stayed ON during a drag → `OrbitControls.autoRotate` fought the finger. Now the showcase
   (`_lockPlacement`) `pointerdown` calls `pauseSpin()` + sets `_interacting=true`.
2. The front/back camera tween (`haveAzTarget`) kept yanking the camera back → snapping. Now cleared on grab.
   Also `G.setAutoSpin` is guarded: `if (on && _interacting) return` so a mid-drag cycle can't re-enable spin.
   On `pointerup`: `_interacting=false` + `scheduleResume(2500)`.
- Verified: **during drag autoRotate=False (no fight), 0 console errors.** ⚠️ Auto-spin *resume* after
  release read inconclusive headless (model-load timing) — **check on a real phone**, then commit + deploy.

**(B) The other two windows' work (in the same tree — keep it):**
- `garment3d.js`: `_frozen` flag + "studio design mode: hold the garment perfectly still".
- `studio.js`: ~+52 lines (wiring the below).
- `studio.css` + `templates/studio.html`: **cycling colour backdrop in the studio** (`.stage__bg`,
  `--studio-bg`, white wash gradients) sharing the landing's BACKDROPS palette.
- `static/media/space-bg.jpg`: **deleted** (studio uses the cycling colour now).

→ Because (A) and (B) are interleaved in `garment3d.js`, a `git add -A` would bundle everyone's work into
one commit. Coordinate: ideally each window commits its own files, or whoever commits names all of it.

## 🔜 Open (documented, not built)
- **Kids range + more garment types** — `PRODUCT_EXPANSION.md`. Use REAL verified Gelato UIDs
  (`build_apparel_catalog.py`) — never fabricate (wrong-product / real-money risk). Kids needs a Meshy GLB
  (the parked `data/designs_parked/penguin.png` is earmarked for it).
- **IP-held designs** kept OUT of the live library pending Luke's OK: Dali, Black Swan, S.HE, Ghost-logo
  (Jackson excluded outright = celebrity).
- Verify spin-resume on device; then commit + deploy (A).

## 💬 Comments (next chat / other windows — add notes here)
-
